<!DOCTYPE html>
<html lang="en">
<head>
  <title>REDDING</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <style>
    /* Remove the navbar's default margin-bottom and rounded borders */
    .navbar {
      margin-bottom: 0;
      border-radius: 0;
    }

    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
    .row.content {height: 450px}

    /* Set gray background color and 100% height */
    .sidenav {
      padding-top: 20px;
      background-color: #f1f1f1;
      height: 100%;
    }

    /* Set black background color, white text and some padding */
    footer {
      background-color: #555;
      color: white;
      padding: 15px;
    }

    /* On small screens, set height to 'auto' for sidenav and grid */
    @media screen and (max-width: 767px) {
      .sidenav {
        height: auto;
        padding: 15px;
      }
      .row.content {height:auto;}
    }
  </style>
</head>
<body>


<div class="container-fluid text-center">
  <div class="row content">
    <div class="col-sm-2 sidenav">
      <br>
      <%= link_to 'Volver al Foro', forums_path %>

    </div>
    <div class="col-sm-8 text-center">

      <center>
      <div class= "card" style="width:400px">
        <img class="card-img-top" src="http://ku.90sjimg.com/element_origin_min_pic/01/40/42/75573cfd6bcd61c.jpg" height="40" width="40" alt="Card image">
        <div class="card-body">
          <h4 class="card-title"><strong>Titulo:</strong><%= @article.title %></h4>
          <p class="card-text"><strong>Cuerpo:</strong><%= @article.body %></p>
          <p class="card-text"><strong>Creador:</strong><%= @article.op %></p>
          <h4 class="card-title"><strong>Comentarios</h4>
          <%= link_to "Nuevo comentario", new_forum_article_comment_path(@forum, @article) %>
          <% @article.comments.each do |comment| %>
            <p class="card-text"><strong><%= comment.op %>:</strong><%= comment.content %></p>
          <% end %>
        </div>
      </div>
    </center>
    </div>
    <div class="col-sm-2 sidenav">
    </div>
  </div>
</div>

<footer class="container-fluid text-center">
  <p>REDDING</p>
</footer>

</body>
</html>
